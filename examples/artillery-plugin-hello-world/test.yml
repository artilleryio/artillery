config:
  target: "http://asciiart.artillery.io:8080"
  phases:
    - arrivalRate: 1
      duration: 10
  plugins:
    hello-world:
      greeting: "Hello world! 👋"
scenarios:
  - flow:
      - get:
          url: "/"