{
  "name": "@artilleryio/types",
  "version": "0.11.0",
  "private": true,
  "files": [
    "./definitions.ts",
    "./schema.json"
  ],
  "license": "MPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/artilleryio/artillery.git",
    "directory": "packages/types"
  },
  "scripts": {
    "pretest": "npm run build",
    "test": "node --test --test-timeout=120000 \"test/**/*.test.ts\"",
    "build": "node generate-schema.js",
    "prepublishOnly": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/tap": "^15.0.8",
    "ajv": "^8.18.0",
    "artillery-plugin-fake-data": "*",
    "joi": "^17.6.0",
    "joi-to-json": "^4.1.0",
    "js-yaml": "^4.3.1",
    "ts-node": "^10.9.1",
    "typescript-json-schema": "^0.59.0"
  }
}
