config:
  target: "http://asciiart.artillery.io:8080"
  phases:
    - duration: 10
      arrivalRate: 1
      name: "Phase 1"
  processor: "./processor.js"

scenarios:
  - flow:
      - function: getFruit