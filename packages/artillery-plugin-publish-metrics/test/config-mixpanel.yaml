config:
  target: https://artillery.io
  phases:
    - duration: 11
      arrivalRate: 1
  plugins:
    publish-metrics:
      - type: mixpanel
        projectToken: "{{ $processEnvironment.MIXPANEL_PROJECTTOKEN }}"
