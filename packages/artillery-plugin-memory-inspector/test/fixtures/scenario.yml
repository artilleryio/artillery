config:
  target: "http://localhost:4444"
  phases:
    - duration: 10
      arrivalRate: 1
      name: "Phase 1"
  processor: "./myProcessor.js"

scenarios:
  - beforeScenario: "myBeforeScenarioHandler"
    flow:
      - get:
          url: "/"